<br />
<div class="booyah-box col-xs-10 col-xs-offset-1">
  <div class="text-center"><h1><%= @course.title %></h1>
    <br/>
    <%= link_to 'Preview As Student...', course_path(@course), class: 'btn btn-warning btn-lg'%>
  </div>
    <br class="clr"/>
    <div class="text-center">
      <%= link_to 'Add a new section...', new_instructor_course_section_path(@course), class: 'btn btn-primary btn-lg' %>
    </div>
    <br/>

    <u1>
      <%= @course.sections.each do |section| %>
        <li>
          <%= section.title %>
        </li>
      <% end %>
    </u1>

</div>
